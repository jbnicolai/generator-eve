language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-eve' ]; then cd .. && eval "mv $currentfolder generator-eve" && cd generator-eve; fi

